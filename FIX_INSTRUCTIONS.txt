// 修复说明：
// Supabase JOIN 查询返回的数据结构：
// {
//   id: "...",
//   name: "酒店名称",
//   city_id: "uuid",
//   cities: {
//     name: "西双版纳"
//   }
// }

// 需要修改的地方：
// 1. 统计城市时：h.city -> h.cities?.name
// 2. 筛选城市时：h.city === cityName -> h.cities?.name === cityName
// 3. 转换数据时：city: h.city -> city: h.cities?.name

// 修改步骤：
// 1. 打开 index.html
// 2. 搜索 "const city = h.city || '未知';"
//    替换为：const city = h.cities?.name || '未知';
// 
// 3. 搜索 "const uniqueCities = [...new Set(dbHotelsData.map(h => h.city))];"
//    替换为：const uniqueCities = [...new Set(dbHotelsData.map(h => h.cities?.name))];
//
// 4. 搜索 "cityHotels = dbHotelsData.filter(h => h.city === cityName);"
//    替换为：cityHotels = dbHotelsData.filter(h => h.cities?.name === cityName);
//
// 5. 搜索 "city: h.city"
//    替换为：city: h.cities?.name
//
// 6. 所有 console.log 中的 h.city 也要改为 h.cities?.name

